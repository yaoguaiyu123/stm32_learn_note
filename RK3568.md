# 前言

硬件设备：正点原子RK3568开发板

https://www.bilibili.com/video/BV1AP411s7iN/

**严禁带电插拔**：绝对不要在板子通电（蓝色灯亮）的情况下插拔摄像头、屏幕排线、跳线帽等外设 。这极易导致接口芯片烧毁。如果需要接线，请先按 ON/OFF 键断电，等灯灭了再操作

![`](.\assets\image-20251231091554400.png)

# 给板子上电

- 使用 HDMI 线连接开发板与显示设备（当前使用支持 HDMI 输入的平板作为显示器）

- 由于我的平板采用软件显示，无触摸屏功能，所以将 USB 鼠标插入开发板左下角的 **USB** 接口，用于系统操作

- 使用套件自带的 **12V/2.5A** 电源适配器 。先将插头插入排插，再将 DC 圆口插入开发板右上角的电源接口 

- 按下电源接口下方的 **ON/OFF** 自锁开关，蓝色电源灯常亮，等待系统启动

# 板子关机

- 在串口终端输入命令：`poweroff`

- 拨动 ON/OFF 开关
- 拔下 12V 电源线
- 拔下HDMI 线、USB......



# 烧入 Linux (Buildroot) 镜像

> 具体步骤见《03【正点原子】ATK-DLRK3568出厂镜像烧录指导V1.2.pdf》
> 我的板子出厂就是 Linux (Buildroot)镜像，但是我误以为是Android，烧录完才发现

- 运行 `DriverAssitant` 安装 Rockchip USB 驱动，确认识别设备

- 打开 **RKDevTool** (瑞芯微开发工具)

- Type-C 数据线，一头插电脑 USB，另一头插开发板左侧的 **OTG** 接口
- 在 RKDevTool 中点击“固件”，选择 **`update(HDMI+MIPI).img`**。选择此镜像既支持当前的 HDMI 显示，也兼容未来可能购买的 MIPI 小屏 

- 让开发板进入 Loader 模式：按住Loader不松，然后按一下复位，让板子进入到Loader模式

- 烧录镜像

# PC终端连接开发板调试串口

使用 **USB-typeC** 连接板子的UART和电脑的USB

![image-20251231104841800](.\assets\image-20251231104841800.png)

# PC Ubuntu20下安装Qt

> 参考《09【正点原子】ATK-DLRK3568_Qt开发环境搭建V1.2.pdf》

## Ubuntu20

> 之前一直用的是Ubuntu24，使用VirtualBox新装一个Ubuntu20

### 踩坑记录

**安装增强功能没有弹出 terminal**

- 发现是 terminal 坏了，进入 `Ctrl` + `Alt` + `F3` 修复
- 删除之前安装到一半的增强功能，重装增强功能

**共享文件夹没有权限，发现是用户权限不够**

- 开机前狂按 `ESC` 进入上帝模式修改用户权限
- 修改共享文件夹权限

### Vritualbox全屏模式

进入全屏模式之后 ==Right Ctrl + F== 返回

## Qt安装变化

**以前 (Qt 5.14 及更早)**

- 离线安装，去官网下载一个几 GB 的 `.run` 或 `.exe` 大包

**现在 (Qt 5.15 及 Qt 6)**

- 在线安装，官网只提供一个几十 MB 的小安装器（`qt-unified-...-online.run`）。需要运行它，注册并登录 Qt 账号，然后它才会联网下载你需要的数据包，官方的离线包现在只提供给付费的商业用户了
- 启动安装器的时候加上==阿里云镜像==：./qt-online-installer-linux-x64-4.10.0.run --mirror https://mirrors.aliyun.com/qt

# Ubuntu20上安装交叉编译工具链

> 参考《09【正点原子】ATK-DLRK3568_Qt开发环境搭建V1.2.pdf》
>
> 参考《33【正点原子】基于Buildroot系统_交叉编译器安装与使用参考手册V1.0.pdf》

- **我的 rk3568 上的 linux 内核是 4.19.232 所以选择**

```bash
atk-dlrk3568-toolchain-arm-buildroot-linux-gnueabihf-x86_64-20230921-v1.0.1.run
```

- **安装的输出**

```bash
vboxuser@Ubuntu20:~/shared_folder$ ./atk-dlrk3568-toolchain-arm-buildroot-linux-gnueabihf-x86_64-20230921-v1.0.1.run
ATK-DLRK356X toolchain installer version 1.0.1 Generated by Buildroot!
======================================================================
Enter target directory for toolchain (default: /opt/atk-dlrk356x-toolchain): 
You are about to install the toolchain to "/opt/atk-dlrk356x-toolchain". Proceed[Y/n]? 
[sudo] password for vboxuser: 
Extracting toolchain........................................................................................done
Relocating the toolchain to /opt/atk-dlrk356x-toolchain...
Toolchain has been successfully set up and is ready to be used.
Each time you wish to use the Toolchain in a new shell session, you need to source the environment setup script e.g.
 $ . export PATH=$PATH:/opt/atk-dlrk356x-toolchain/usr/bin
```

- **配置编译器全局环境变量**

```bash
echo 'export PATH=$PATH:/opt/atk-dlrk356x-toolchain/usr/bin' >> ~/.bashrc
source ~/.bashrc
```

- **获取编译器名称**

```bash
vboxuser@Ubuntu20:~/shared_folder$  ls /opt/atk-dlrk356x-toolchain/usr/bin/*gcc
/opt/atk-dlrk356x-toolchain/usr/bin/aarch64-buildroot-linux-gnu-gcc
/opt/atk-dlrk356x-toolchain/usr/bin/aarch64-linux-gcc
```

输出两个名称，但是实际上`aarch64-linux-gcc` 是 `aarch64-buildroot-linux-gnu-gcc` 的软链接

- **验证编译器版本**

```bash
 aarch64-linux-gcc -v
```

打印输出如下，其中 `gcc` 是 10.3.0

```bash
使用内建 specs。
COLLECT_GCC=/opt/atk-dlrk356x-toolchain/bin/aarch64-linux-gcc.br_real
COLLECT_LTO_WRAPPER=/opt/atk-dlrk356x-toolchain/bin/../libexec/gcc/aarch64-buildroot-linux-gnu/10.3.0/lto-wrapper
目标：aarch64-buildroot-linux-gnu
配置为：./configure --prefix=/home/alientek/ATK-DLRK3568/buildroot/output/rockchip_rk3568/host --sysconfdir=/home/alientek/ATK-DLRK3568/buildroot/output/rockchip_rk3568/host/etc --enable-static --target=aarch64-buildroot-linux-gnu --with-sysroot=/home/alientek/ATK-DLRK3568/buildroot/output/rockchip_rk3568/host/aarch64-buildroot-linux-gnu/sysroot --enable-__cxa_atexit --with-gnu-ld --disable-libssp --disable-multilib --disable-decimal-float --with-gmp=/home/alientek/ATK-DLRK3568/buildroot/output/rockchip_rk3568/host --with-mpc=/home/alientek/ATK-DLRK3568/buildroot/output/rockchip_rk3568/host --with-mpfr=/home/alientek/ATK-DLRK3568/buildroot/output/rockchip_rk3568/host --with-pkgversion='Buildroot 2018.02-rc3-g30e3a128' --with-bugurl=http://bugs.buildroot.net/ --without-zstd --disable-libquadmath --disable-libquadmath-support --enable-tls --enable-threads --without-isl --without-cloog --with-abi=lp64 --with-cpu=cortex-a55 --enable-languages=c,c++ --with-build-time-tools=/home/alientek/ATK-DLRK3568/buildroot/output/rockchip_rk3568/host/aarch64-buildroot-linux-gnu/bin --enable-shared --disable-libgomp
线程模型：posix
Supported LTO compression algorithms: zlib
gcc 版本 10.3.0 (Buildroot 2018.02-rc3-g30e3a128) 

```

# 命令行交叉编译Qt项目（qmake）

> 见《09【正点原子】ATK-DLRK3568_Qt开发环境搭建V1.2.pdf》

```bash
# 查看qmake版本
/opt/atk-dlrk356x-toolchain/usr/bin/qmake -v

```



# QtCreater交叉编译Qt项目（qmake）

> 见《09【正点原子】ATK-DLRK3568_Qt开发环境搭建V1.2.pdf》

## 命名规则

配置 `Kits` 的时候发现的命名规则

| **名字**             | **全称 / 含义**             | **里的字母代表什么？**               |
| -------------------- | --------------------------- | ------------------------------------ |
| **GCC** (大写项目名) | GNU Compiler **Collection** | **C** = **Collection** (集合/全家桶) |
| **gcc** (小写命令)   | GNU **C** Compiler          | **c** = **C Language** (C语言)       |
| **g++** (小写命令)   | GNU **C++** Compiler        | **++** = **C++ Language** (C++语言)  |

## 切换构建套件

点击左下角的🖥️按钮进行切换

![image-20260101071708069](.\assets\image-20260101071708069.png)

# 远程部署

## RK3568 开发板 WiFi 联网（手机热点版）

**当前环境：** 没网线，也没路由器。 思路：让电脑、平板、开发板全都连我手机热点，组成一个局域网

**遇到的坑：** 刚开始死活连不上，报错 `Operation not possible due to RF-kill`。 查了下是因为系统为了省电，默认开机把 WiFi 给“软锁死”了（类似飞行模式），必须先手动解锁！

**操作流程：**

- **准备工作**：先把天线拧上（接 WIFI_ANT1），

- **改配置**： `vi /etc/wpa_supplicant.conf` 填入手机热点账号密码

    ```bash
    network={
       ssid="GOOD"
       psk="66668888"
       key_mgmt=WPA-PSK
    }
    ```

- **解锁 & 连接**：

    ```bash
    # 解除省电锁定
    rfkill unblock all
    
    # 清理旧进程
    killall wpa_supplicant  
    
    # 连接
    wpa_supplicant -B -i wlan0 -c /etc/wpa_supplicant.conf  
    
    # 查看IP
    udhcpc -i wlan0   
    ```

- **检查状态**

    ```bash
    # 查看IP
    ifconfig wlan0
    
    ping baidu.com
    ```

### 已经将上述步骤写为一个开机自启动的脚本

```bash
vi /etc/init.d/S99wbywifi
```



## QtCreater配置远程设备

> 见《09【正点原子】ATK-DLRK3568_Qt开发环境搭建V1.2.pdf》

## 部署之后（==尝试基于Weston显示，失败，无法全屏==）

>  正点原子出厂自带了一个“手机桌面”（SystemUI），运行在 Weston（Wayland 合成器）之上，我的程序并没有独占

### 禁用原厂启动脚本

进入启动脚本目录，将原厂界面的脚本改名（使其不以 `S` 开头），从而实现禁用。

```bash
cd /etc/init.d/
mv S50systemui off_S50systemui   # 禁用 SystemUI (手机界面)
```

### 加入我的脚本

```bash
vi S99wbyapp
```

### 脚本内容如下

```
#!/bin/sh
#
# Start MotoDashboard (Robust Wayland Client Mode)
#

# 定义程序路径和 socket 路径
APP_PATH="/opt/demo01/bin/demo01"
SOCKET_DIR="/run"
SOCKET_FILE="wayland-0"

case "$1" in
  start)
    echo "Starting Moto Dashboard..."

    # ==========================================
    # 1. 核心防御：等待 Weston 就绪 (最多等 10 秒)
    # ==========================================
    # 刚刚烧录完的系统，第一次启动 Weston 可能会慢一点
    # 我们这里写个循环，死等 /run/wayland-0 出现
    
    count=0
    while [ ! -e "$SOCKET_DIR/$SOCKET_FILE" ]; do
        if [ $count -gt 50 ]; then
            echo "Error: Weston socket not found after 10s, giving up."
            exit 1
        fi
        echo "Waiting for Wayland socket..."
        sleep 0.2
        count=$((count+1))
    done

    echo "Wayland socket found! Launching..."

    # ==========================================
    # 2. 设置环境变量
    # ==========================================
    export QT_QPA_PLATFORM=wayland
    export XDG_RUNTIME_DIR=$SOCKET_DIR
    export WAYLAND_DISPLAY=$SOCKET_FILE
    
    # 这一行是为了防止有些 QT 程序字体发虚，可选
    export QT_SCALE_FACTOR=1 

    # ==========================================
    # 3. 启动程序 (带“防弹衣”)
    # ==========================================
    # > /dev/null 2>&1 &  <-- 这是保命符，确保后台运行且不污染终端
    if [ -x "$APP_PATH" ]; then
        $APP_PATH > /dev/null 2>&1 &
        echo "Moto Dashboard started."
    else
        echo "Error: Application not found at $APP_PATH"
    fi
    ;;

  stop)
    echo "Stopping Moto Dashboard..."
    killall -q demo01
    ;;

  restart|reload)
    "$0" stop
    sleep 1
    "$0" start
    ;;

  *)
    echo "Usage: $0 {start|stop|restart}"
    exit 1
esac

exit 0
```

### 添加执行权限

````bash
chmod +x /etc/init.d/S99wbyapp
````

### 发现UI还是被限制（==还未改回来==）

`/etc/xdg/weston/weston.ini`文件

```
[core]
backend=drm-backend.so

# Allow running without input devices
require-input=false

# Disable screen idle timeout by default
idle-time=0

# The repaint-window is used to calculate repaint delay(ms) after flipped.
#   value <= 0: delay = abs(value)
#   value > 0: delay = vblank_duration - value
repaint-window=-1

# Allow blending with lower drm planes
# gbm-format=argb8888

[shell]
# top(default)|bottom|left|right|none, none to disable panel
panel-position=none

# Scale panel size
panel-scale=3

# none|minutes(default)|minutes-24h|seconds|seconds-24h
# clock-format=minutes-24h
clock-with-date=false

# Disable screen locking
locking=false

[libinput]
# Uncomment below to enable touch screen calibrator(weston-touch-calibrator)
# touchscreen_calibrator=true
# calibration_helper=/bin/weston-calibration-helper.sh

[keyboard]
# Comment this to enable vt switching
vt-switching=false

# Configs for auto key repeat
# repeat-rate=40
# repeat-delay=400

# [output]
# name=LVDS-1
# transform=rotate-90
```

修改为

```
[core]
backend=drm-backend.so
require-input=false
idle-time=0
repaint-window=-1

[shell]
panel-position=none
panel-scale=3
clock-with-date=false
locking=false

[keyboard]
vt-switching=false

[output]
mode=preferred
transform=normal

```

## 部署之后（基于EGLFS 显示）

```bash
cd /etc/init.d/

# 关闭 weston 自启动
mv S49weston off_S49weston  

vi S99wbyapp
```

### S99wbyapp

```
root@ATK-DLRK3568:/etc/init.d# cat ./S99mydashboard
#!/bin/sh
#
# Start MotoDashboard
#

case "$1" in
  start)
    echo "Starting Moto Dashboard..."

    # 1. 基础环境变量
    # 告诉 Qt 使用 eglfs 插件，直接接管屏幕
    export QT_QPA_PLATFORM=eglfs
    # 隐藏鼠标指针
    export QT_QPA_EGLFS_HIDECURSOR=1

    # 2. 修复日志里的 QStandardPaths 警告
    # 给 Qt 指定一个可写的临时目录，消除 runtime directory 报错
    export XDG_RUNTIME_DIR=/tmp/runtime-root
    mkdir -p $XDG_RUNTIME_DIR
    chmod 700 $XDG_RUNTIME_DIR

    # 3. 后台运行你的程序
    /opt/MotoDashboard/bin/MotoDashboard &
    ;;

  stop)
    echo "Stopping Moto Dashboard..."
    killall MotoDashboard
    ;;

  restart|reload)
    "$0" stop
    "$0" start
    ;;

  *)
    echo "Usage: $0 {start|stop|restart}"
    exit 1
esac

exit 0
root@ATK-DLRK3568:/etc/init.d#

```

## QtCreater突然部署失败

- 去“工具-外部-配置-设备”里面重跑一次该设备的测试



# 开发板上的蓝牙服务

**蓝牙协议：** Ready (BlueZ 5 + PulseAudio Modules)

```bash
# 检查蓝牙是否在运行
bluetoothctl show              # Powered: yes

# 查看蓝牙配对的所有设备
bluetoothctl devices

# 查看蓝牙已连接的所有设备
bluetoothctl list

# 查看蓝牙已连接的设备和开发板的主从关系
hcitool con
```



# 开发板上的音频服务

**音频中枢：** Ready (PulseAudio 14.2) 

```bash
# 查看当前蓝牙耳机的音量
pactl list sinks | grep -A 10 "bluez_sink.11_02_03_06_13_5D.a2dp_sink"

# 调节音量设置到30%
pactl set-sink-volume bluez_sink.11_02_03_06_13_5D.a2dp_sink 30%
```



# 手机➕开发板➕蓝牙耳机的连接

> 参考《01【正点原子】ATK-DLRK3568_Buildroot系统快速体验手册V1.3.pdf》3.11章节

## 初始化蓝牙

```bash
# 正点原子提供的蓝牙初始化的脚本
/root/shell/bluetooth/bt_init.sh

bluetoothctl

power on # 打开电源
agent on # 开启代理
discoverable on # 开启可被检测
scan on # 开启扫描设备
```

## 扫描得到的耳机的信息

```bash
[CHG] Device 11:02:03:06:13:5D EirData:
  0f 09 53 74 61 72 52 69 6e 67 20 55 6c 74 72 61  ..StarRing Ultra
  13 03 00 10 0e 11 0f 11 0c 11 0b 11 1e 11 03 12  ................
  00 12 01 11 01 05 01 07 04 0d 04 04 24 00 00 00  ............$...
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................
  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  ................

```

- 发起配对

```
pair 11:02:03:06:13:5D
```

- 蓝牙耳机配对成功

![image-20260102152619506](.\assets\image-20260102152619506.png)

- 连接蓝牙耳机

```bash
connect 11:02:03:06:13:5D
trust 11:02:03:06:13:5D

aplay /usr/share/sounds/test.wav   # 测试音频
paplay /usr/share/sounds/test.wav   # 测试音频

pactl list short sinks    # 检查 PulseAudio 是否识别耳机

# 设置蓝牙耳机为默认 sink
pactl set-default-sink bluez_sink.11_02_03_06_13_5D.a2dp_sink   
```

> 耳机 MAC: 11:02:03:06:13:5D (StarRing Ultra)
>
> Sink 名字: bluez_sink.11_02_03_06_13_5D.a2dp_sink



## 手机连接开发板

![image-20260102162156704](.\assets\image-20260102162156704.png)

![image-20260102154722829](.\assets\image-20260102154722829.png)

```bash

root@ATK-DLRK3568:/# pactl list short sources
0       alsa_output.1.stereo-fallback.monitor   module-alsa-card.c      s16le 2ch 8000Hz        SUSPENDED
1       alsa_input.1.stereo-fallback    module-alsa-card.c      s16le 2ch 8000Hz        SUSPENDED
2       alsa_output.2.HDMI__hw_rockchiphdmi__sink.monitor       module-alsa-card.c      s16le 2ch 44100Hz       SUSPENDED
3       alsa_output.0.HiFi__hw_rockchiprk809co__sink.monitor    module-alsa-card.c      s16le 2ch 48000Hz       SUSPENDED
4       alsa_input.0.HiFi__hw_rockchiprk809co__source   module-alsa-card.c      s16le 2ch 44100Hz       SUSPENDED
5       bluez_sink.11_02_03_06_13_5D.a2dp_sink.monitor  module-bluez5-device.c  s16le 2ch 44100Hz       SUSPENDED
6       bluez_source.3C_38_24_BF_48_EC.a2dp_source      module-bluez5-device.c  s16le 2ch 44100Hz       SUSPENDED
root@ATK-DLRK3568:/#

```

**耳机相关**： 5 bluez_sink.11_02_03_06_13_5D.a2dp_sink.monitor 这就是耳机的**monitor source**（sink 的监听源），它代表耳机作为输出设备时产生的“反馈”通道。虽然主要用它作为 sink，但 monitor 也会出现在 sources 列表里

**手机相关**： 6 bluez_source.3C_38_24_BF_48_EC.a2dp_source 这正是我的 Xiaomi 15 手机作为 **A2DP Source**（音频发送端），开发板接收到的手机音频源。 当你播放音乐后，它从 SUSPENDED 自动激活（变成 RUNNING 或 IDLE），音频流就通过 loopback 模块实时转发到耳机了

**手机MAC地址**：`3C:38:24:BF:48:EC`

## 最终效果

- 手机播放的声音能够通过开发板中转传达到耳机，但是手机端的调节音量无效

- 整个配置过程还是比较顺利，通过以下的命令在开发板上列出当前 PulseAudio 正在加载的所有模块（modules），然后从中筛选出名字里包含 “switch”、“bluetooth”、“stream” 或 “role” 这四个关键词的模块
- 系统采用 PulseAudio 的 module-bluetooth-policy、module-switch-on-port-available 等模块实现蓝牙设备的自动发现、profile 切换和音频路由。手机 A2DP Source 音频流在检测到耳机 A2DP Sink 后自动路由，无需额外 loopback 配置，实现了低延迟音乐中继与导航语音优先播报

```bash

root@ATK-DLRK3568:/# pactl list modules short | grep -E "switch|bluetooth|stream|role"
1       module-stream-restore
4       module-switch-on-port-available
9       module-bluetooth-policy
10      module-bluetooth-discover
18      module-intended-roles
21      module-role-cork
```

## 解决播放音频的时候蓝牙日志刷屏

```bash
dmesg -n 4
```

- 创建开机自启动脚本然后添加进去

```bash
vi /etc/init.d/S99wby_init       # 我的开机自启动脚本
```

## 自动重连的效果

**关于蓝牙耳机：**在开发板未做任何编码的情况下，重启之后，蓝牙耳机再开机，则可以自动连接，但是**没有自动设置蓝牙耳机为默认音频输出设备**

```bash
# 快速查看当前默认sink
pactl info | grep "Default Sink"

# 设置蓝牙耳机为默认音频输出设备
pactl set-default-sink bluez_sink.11_02_03_06_13_5D.a2dp_sink   
```

**关于手机：**当手机的蓝牙重启，是可以自动连接开发板（手机➡️开发板），但是**开发板重启无法自动重连**

### 设置蓝牙自启动

编辑`/etc/init.d/S99wby_init`将正点原子的**蓝牙初始化脚本**添加进去

然后重启之后验证是否启动

```bash
# 检查蓝牙是否在运行
bluetoothctl show              # Powered: yes
```

### 当有新连接到来，检测音频输出之后设置该设备为默认音频输出设备

```bash
# 添加自动检测的脚本
vi /root/auto_set_default_sink.sh

# 添加该脚本的运行
/etc/init.d/S99wby_init
```

````bash
#!/bin/sh
# /root/auto_set_default_sink.sh
# Automatically set Bluetooth audio devices as default sink
# Improved version with better detection and retry logic

LOG_FILE="/var/log/bt_audio_sink.log"
LOCK_FILE="/var/run/bt_sink_monitor.lock"

# Prevent multiple instances
if [ -f "$LOCK_FILE" ]; then
    echo "$(date): Script already running (PID: $(cat $LOCK_FILE))" >> "$LOG_FILE"
    exit 1
fi
echo $$$$ > "$LOCK_FILE"
trap "rm -f $LOCK_FILE; exit" INT TERM EXIT

# Log function
log() {
    echo "$(date '+%Y-%m-%d %H:%M:%S') - $1" >> "$LOG_FILE"
    echo "$1"  # Also print to console for debugging
}

# Wait for PulseAudio
wait_for_pulse() {
    local count=0
    while [ $count -lt 30 ]; do
        if pactl info >/dev/null 2>&1; then
            log "PulseAudio ready"
            return 0
        fi
        sleep 1
        count=$((count + 1))
    done
    log "ERROR: PulseAudio not ready after 30s"
    return 1
}

# Check if sink is Bluetooth A2DP
is_bluetooth_sink() {
    local sink="$1"
    # Match bluez_sink.MAC.a2dp_sink format
    if echo "$sink" | grep -E "^bluez_sink_[0-9a-f]{2}_[0-9a-f]{2}_[0-9a-f]{2}_[0-9a-f]{2}_[0-9a-f]{2}_[0-9a-f]{2}_a2dp_sink$" >/dev/null 2>&1; then
        return 0
    fi
    # Also match the colon format if it exists
    if echo "$sink" | grep -q "bluez_sink.*a2dp_sink"; then
        return 0
    fi
    return 1
}

# Set default sink with retry
set_default_sink() {
    local sink="$1"
    local max_retry=5
    local retry=0

    while [ $retry -lt $max_retry ]; do
        if pactl set-default-sink "$sink" 2>/dev/null; then
            log "SUCCESS: Set default sink to $sink"

            # Set volume to 70%
            pactl set-sink-volume "$sink" 70% 2>/dev/null
            pactl set-sink-mute "$sink" 0 2>/dev/null
            log "Volume set to 70% and unmuted"
            return 0
        else
            retry=$((retry + 1))
            log "Retry $retry/$max_retry: Failed to set $sink"
            sleep 2
        fi
    done

    log "ERROR: Failed to set $sink after $max_retry attempts"
    return 1
}

# Main monitoring using polling (more reliable than events)
monitor_by_polling() {
    log "Starting polling monitor"

    # Get initial sinks
    get_sinks() {
        pactl list short sinks 2>/dev/null | awk '{print $2}' | grep -E "bluez_sink.*a2dp_sink" | sort
    }

    prev_sinks=$(get_sinks)

    while true; do
        sleep 5  # Check every 5 seconds

        current_sinks=$(get_sinks)

        # Find new sinks
        new_sinks=$(comm -13 <(echo "$prev_sinks") <(echo "$current_sinks") 2>/dev/null)

        if [ -n "$new_sinks" ]; then
            log "New Bluetooth sinks detected: $new_sinks"

            for sink in $new_sinks; do
                # Wait for sink to be fully ready
                sleep 3
                set_default_sink "$sink"
            done

            prev_sinks="$current_sinks"
        fi
    done
}

# Alternative: Monitor udev events
monitor_udev() {
    log "Starting udev monitor"

    udevadm monitor --subsystem-match=bluetooth --environment | while read -r line; do
        if echo "$line" | grep -q "ACTION=add"; then
            log "Bluetooth device added via udev"
            sleep 4  # Wait for PulseAudio registration

            # Check for new Bluetooth sinks
            sinks=$(pactl list short sinks 2>/dev/null | grep "bluez_sink.*a2dp_sink" | awk '{print $2}')
            for sink in $sinks; do
                log "Found sink via udev: $sink"
                set_default_sink "$sink"
            done
        fi
    done
}

# Main execution
main() {
    log "=== Bluetooth Sink Monitor Started (Improved) ==="

    if ! wait_for_pulse; then
        exit 1
    fi

    # Start both monitoring methods
    monitor_by_polling &
    POLL_PID=$!

    monitor_udev &
    UDEV_PID=$!

    log "Monitor PIDs: poll=$POLL_PID, udev=$UDEV_PID"

    # Wait for any child to exit
    wait

    log "ERROR: Monitor process exited"
    exit 1
}

main



root@ATK-DLRK3568:/#
````

### ==当开发板重启之后，尝试去连接耳机和手机==

> 一直会报错➕声音卡顿，先不尝试了
>
> 主动连接/广播mac地址被动连接，都不行

```bash
vi /root/bt_connect_hub.sh
rm -f /root/bt_connect_hub.sh
chmod +x /root/bt_connect_hub.sh


vi /root/testc.sh
rm -f /root/testc.sh
chmod +x /root/testc.sh
/root/testc.sh

bluetoothctl connect 3C:38:24:BF:48:EC
```

```bash
#!/bin/sh
# 脉冲唤醒版

log() { echo "$(date '+%H:%M:%S') - $1"; }

# 设置 CPU 性能模式，确保 UART 有足够的处理优先级
echo performance > /sys/devices/system/cpu/cpufreq/policy0/scaling_governor

# 开启广播
bluetoothctl discoverable on
bluetoothctl pairable on

# 获取配对列表并仅发起一次唤醒
PAIRED=$(bluetoothctl paired-devices | awk '{print $2}')
for mac in $PAIRED; do
    if ! bluetoothctl info "$mac" | grep -q "Connected: yes"; then
        log "正在尝试唤醒: $mac"
        # 严格限制连接指令的存活时间，防止其在后台与音频流竞争 UART 总线
        timeout 5s bluetoothctl connect "$mac" >/dev/null 2>&1 & 
        sleep 2
    fi
done

# 保持 30 秒广播后退出，让系统回归纯净的音频传输状态
sleep 30
bluetoothctl discoverable off
log "连接引导任务结束"
```



# 手机➕开发板➕蓝牙耳机的连接（开发板作为绝对主机）

我本以为上述的连接是手机（主机）➡️开发板（主机/从机之间切换）➡️耳机（从机）的设定，但是发现即使是手机去主动连接的开发板，手机依然是从机，猜测是发生了**角色对换**

https://www.tarlogic.com/bsam/controls/bluetooth-role-switch-authentication/

#  尝试解决蓝牙连接的音频传输问题

> 参考《01【正点原子】ATK-DLRK3568嵌入式Linux系统开发手册V1.3.pdf》
>
> 尝试自编译镜像来解决

# 开发qt车载仪表盘项目

![摩托车仪表盘指示灯大全- 抖音](https://p3-pc-sign.douyinpic.com/tos-cn-i-0813/oEmACo4sgCgIFAF9yAVQOcHAA3AZgffGNbrDEg~tplv-dy-aweme-images:q75.webp?biz_tag=aweme_images&from=327834062&lk3s=138a59ce&s=PackSourceEnum_SEARCH&sc=image&se=false&x-expires=1777453200&x-signature=9atXuMuN743HImdJDvaVkui7M9U%3D)



## Qt程序部署之后快速切换到EGLFS显示

```bash
/opt/MotoDashboard/bin/MotoDashboard  # 这个默认使用Wayland，会启动失败
```



```bash
# 已经写好的脚本，使用EGLFS方式启动
vi /root/run_qt.sh

/root/run_qt.sh start

/root/run_qt.sh stop
```

## 查看开发版是否已经连接耳机

```bash
bluetoothctl devices

bluetoothctl info 11:02:03:06:13:5D 

bluetoothctl pair 11:02:03:06:13:5D
bluetoothctl connect 11:02:03:06:13:5D


[bluetooth]# pair 11:02:03:06:13:5D
Attempting to pair with 11:02:03:06:13:5D
[CHG] Device 11:02:03:06:13:5D Connected: yes
[StarRing Ultra]# [ 6223.738338] Bluetooth: hci0: security requested but not available
[CHG] Device 11:02:03:06:13:5D MTU: 0x01ef
[CHG] Device 11:02:03:06:13:5D MTU: 0x01ef
[CHG] Device 11:02:03:06:13:5D Name: StarRing Ultra_BLE
[CHG] Device 11:02:03:06:13:5D Alias: StarRing Ultra_BLE
[CHG] Device 11:02:03:06:13:5D ServicesResolved: yes
[StarRing Ultra]# connect 11:02:03:06:13:5D
Attempting to connect to 11:02:03:06:13:5D
[CHG] Device 11:02:03:06:13:5D Name: StarRing Ultra
[CHG] Device 11:02:03:06:13:5D Alias: StarRing Ultra
Connection successful
[StarRing Ultra]#



bluetoothctl disconnect 11:02:03:06:13:5D  # 断开连接
```



## 按键控制

```kotlin
Item {
        focus: true // 必须设置
        anchors.fill: parent
        Keys.enabled: true

        Keys.onPressed: {
            console.log("Key Pressed:", event.key)
            var currentName = stackView.currentItem ? stackView.currentItem.objectName : ""

            switch (event.key) {
                // 1. MENU: 循环切换界面
                case 16777436:
                    console.log("Menu!!!")
                    cyclePage()
                    event.accepted = true
                    break

                // 2. ESC/Back: 返回上一级
                case 16777216:
                    if (stackView.depth > 1) {
                        stackView.pop()
                    }
                    event.accepted = true
                    break

                // 3. V+: 音量加 OR 开始扫描
                case 16777328:
                    if (currentName === "bluetooth") {
                        // 如果在蓝牙界面，调用该界面的 startScanning 方法
                        if (stackView.currentItem.startScanning) {
                            stackView.currentItem.startScanning()
                        }
                    } else {
                        // 其他界面调节音量
                        changeVolume(5)
                    }
                    event.accepted = true
                    break

                // 4. V-: 音量减 OR 停止扫描
                case 16777330:
                    if (currentName === "bluetooth") {
                        // 如果在蓝牙界面，调用该界面的 stopScanning 方法
                        if (stackView.currentItem.stopScanning) {
                            stackView.currentItem.stopScanning()
                        }
                    } else {
                        // 其他界面调节音量
                        changeVolume(-5)
                    }
                    event.accepted = true
                    break
            }
        }
    }
```



## 看不到调试信息

> 重启即可，脚本中默认是在后台运行，放到前台即可

```bash
killall MotoDashboard
/opt/MotoDashboard/bin/MotoDashboard
```

## 车辆硬件信息模拟

```bash
aarch64-buildroot-linux-gnu-gcc pre_data.c -o pre_data

ifconfig wlan0   # 查看IP
# ssh root@10.87.105.22    # 登录板子
scp pre_data root@10.87.105.22:/opt/   # 传输可执行文件到板子

/opt/pre_data    # 该程序用于在共享内存生成模拟数据

```

## 项目中蓝牙开发的思路

### 监听配对，发起配对

另开一个bluetoothctl（本质是一个蓝牙的shell）进程完成

### SSP数据通道

使用qt的库完成

### 经典蓝牙的各个协议

| **协议名称** | **全称**                            | **用途**                | **状态**          |
| ------------ | ----------------------------------- | ----------------------- | ----------------- |
| **A2DP**     | Advanced Audio Distribution Profile | 传音乐、传声音          | **已连接 (默认)** |
| **HFP/HSP**  | Hands-Free / Headset Profile        | 传通话、来电提示        | ==不支持==        |
| **AVRCP**    | Audio/Video Remote Control          | 传歌名、控制播放        | **已连接 (默认)** |
| **SPP**      | **Serial Port Profile**             | **传自定义数据 (JSON)** | ==默认不连接==    |

## 开发板发送信息到手机

> 音频使用默认的 ADP2协议传输 
>
>  蓝牙音频（A2DP）连接后，手机会自动推流；但 **SPP 数据通道 必须由客户端（手机 App）显式发起 `connect`**

```bash
sdptool browse local    

discoverable on

# 开启蓝牙对SPP的支持，添加到了 /root/shell/bluetooth/bt_init.sh 中，用于开机自启动
sdptool add SP


```

| `speed` | 车速       |
| ------- | ---------- |
| `rpm`   | 发动机转速 |
| `gear`  | 当前档位   |
| `fuel`  | 剩余油量   |
| `range` | 剩余续航   |
| `odo`   | 总里程     |
| `volt`  | 电池电压   |
| `eTemp` | 发动机温度 |
| `aTemp` | 环境温度   |



## 开发版接收歌曲信息

```bash
# 手机基本信息
bluetoothctl
devices
info 3C:38:24:BF:48:EC

# 查看歌曲信息
gdbus call --system \
  --dest org.bluez \
  --object-path / \
  --method org.freedesktop.DBus.ObjectManager.GetManagedObjects | grep -i 3C_38_24_BF_48_EC
  
  
gdbus introspect --system \
  --dest org.bluez \
  --object-path /org/bluez/hci0/dev_3C_38_24_BF_48_EC/player0

gdbus call --system \
  --dest org.bluez \
  --object-path /org/bluez/hci0/dev_3C_38_24_BF_48_EC/player0 \
  --method org.freedesktop.DBus.Properties.Get org.bluez.MediaPlayer1 Track

gdbus call --system \
  --dest org.bluez \
  --object-path /org/bluez/hci0/dev_3C_38_24_BF_48_EC/player0 \
  --method org.freedesktop.DBus.Properties.Get org.bluez.MediaPlayer1 Status
  
# 持续监听
dbus-monitor --system "sender='org.bluez'"


# 暂停 -> 播放
gdbus call --system \
  --dest org.bluez \
  --object-path /org/bluez/hci0/dev_3C_38_24_BF_48_EC/player0 \
  --method org.bluez.MediaPlayer1.Play

# 播放 -> 暂停
gdbus call --system \
  --dest org.bluez \
  --object-path /org/bluez/hci0/dev_3C_38_24_BF_48_EC/player0 \
  --method org.bluez.MediaPlayer1.Pause
  
# 下一首
gdbus call --system \
  --dest org.bluez \
  --object-path /org/bluez/hci0/dev_3C_38_24_BF_48_EC/player0 \
  --method org.bluez.MediaPlayer1.Next
```



## 开发版接收来电信息/接收队友的发送的相关信息

当前 RK3568 板子的蓝牙音频栈只具备 A2DP 媒体音频能力，没有可工作的 HFP/HSP 电话音频后端；因此手机来电时无法建立通话音频链路，只能播放音乐，不能播放电话音频。



```bash
# 开发版不支持开发版接收来电信息，让队友走SPP发送给我

# 来电
{
  "type": "call",
  "state": "ringing",
  "number": "13812345678",
  "name": "张三",
  "location": "浙江 杭州"    # 归属地
}

{"type": "call","state": "ringing","number": "13812345678","name": "张三","location": "浙江 杭州"}

{
  "type": "call",
  "state": "ringing",
  "number": "13812345678",
  "name": "张三",
  "location": "浙江 杭州"    # 归属地
}

# 挂断
{
  "type": "call",
  "state": "idle",    
  "number": "13812345678"
}

{"type": "call","state": "idle", "number": "13812345678"}

# 导航（导航的声音可以直接传输）
{
    "type":"nav",
    "action":"left",   # 左转
    "dist":"150",        # 距离
    "street":"深南大道"    # 下一条路
}


{"type":"nav","action":"left","dist":"150"}
{"type":"call","state":"ringing","number":"13812345678","name":"张三","location":"浙江 杭州"}
{"type":"call","state":"idle","number":"13812345678"}
```



## Qt程序自启动

**开机之后自动启动仪表盘**

```bash

/root/run_qt.sh
/opt/pre_data 


root@ATK-DLRK3568:/etc/init.d

cd /etc/init.d

# vi ./S99z_qt



```

## 自动重启之后遇到问题

板子重启，qt启动，蓝牙连接，音乐播放 ==信息显示失败==

板子重启，qt启动，蓝牙连接，qt重启（等待）  ==信息显示成功==

### 原因

第一次开机启动时，`BTMusicController` 去找 `player0` 的时机太早了

`findPlayerPath()` 失败

`m_playerPath` 为空

后面不再重试

### 已通过Timer多次轮询解决

## 自定义脚本

> 位置在`cd /etc/init.d/`

# 升级开发环境：VSCODE ➕ SSH

## 配置Ubuntu的ssh

https://zhuanlan.zhihu.com/p/681363165

```bash
# ssh服务开机自启动
sudo systemctl enable ssh

# ssh服务默认运行在 22 端口
sudo systemctl status ssh

# 我的项目路径
/home/vboxuser/qt_projects/MotoDashboard

# 生成密钥对
ssh-keygen -t ed25519 -C "ubuntu-to-rk3568"

# 将公钥拷贝到开发板（开发板的IP每次都不一样）
ssh-copy-id root@10.84.181.22

# 登录开发板（开发板的IP每次都不一样）
ssh root@10.84.181.22
```

## 创建.vscode/task.json

```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "1. qmake 配置交叉编译",
            "type": "shell",
            "command": "cd build/RK3568 && qmake ../../MotoDashboard.pro",
            "group": "build",
            "problemMatcher": []
        },
        {
            "label": "2. 编译 MotoDashboard",
            "type": "shell",
            "command": "cd build/RK3568 && make -j$(nproc)",
            "dependsOn": "1. qmake 配置交叉编译",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        },
        {
            "label": "3. Clean + Full Build",
            "type": "shell",
            "command": "cd build/RK3568 && make clean && qmake ../../MotoDashboard.pro && make -j$(nproc)",
            "group": "build",
            "problemMatcher": ["$gcc"]
        },
        {
            "label": "4. 仅部署可执行文件",
            "type": "shell",
            "command": "scp build/RK3568/MotoDashboard root@10.84.181.22:/opt/MotoDashboard/bin/MotoDashboard",
            "dependsOn": "2. 编译 MotoDashboard",
            "problemMatcher": []
        },
        {
            "label": "5. 部署并运行",
            "type": "shell",
            "command": "ssh root@10.84.181.22 'killall MotoDashboard || true; sleep 1' && scp build/RK3568/MotoDashboard root@10.84.181.22:/opt/MotoDashboard/bin/MotoDashboard && ssh root@10.84.181.22 'chmod +x /opt/MotoDashboard/bin/MotoDashboard && /opt/MotoDashboard/bin/MotoDashboard'",
            "dependsOn": "2. 编译 MotoDashboard",
            "group": "test",
            "problemMatcher": []
        }
    ]
}
```

`Ctrl+Shift+M` 选择任务进行编译，这是我自定义的快捷键

或者`Ctrl+Shift+P`然后选择 `Tasks:Run Task`

## 创建.vscode/c_cpp_properties.json

用于cpp错误提示

## 之后每次开发要做的

```bash
# ubuntu登录开发板（开发板的IP每次都不一样）
ssh root@10.84.181.22

# 在开发板上获取IP
ifconfig wlan0  
```

然后修改.vscode/task.json文件里面的IP相关的东西，就可以开始开发了

## 不足

vscode对于cpp的提示还行，但是对于qml的错误提示很弱

# 升级开发环境：QtCreater➕ SSH

https://doc.qt.io/qtcreator/creator-how-to-add-custom-compilers.html

https://doc.qt.io/qtcreator/creator-how-to-build-on-remote-linux.html

> 失败了，以后有空再研究

# Buildroot



# CodeX使用

先选择一个文件夹（工作目录）

